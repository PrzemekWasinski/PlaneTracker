# Plane Tracker

This is my personal project which uses a Raspberry Pi 3 connected to an ADSB receiver which catches plane signals, sends
their information to Firease and lastly it is retrieved by this Kotlin app. The app sends the user a notification every 
10min by default about the most recent plane information and also displays it on the app's main page.
